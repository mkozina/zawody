<script src="jquery.min.js"></script>
<script src="js/ioref.js"></script>

<h1 id="contestname"><img id="contestStatus" src="img/bullet_red.png" width="35" />
<%= contest.name %>
</h1>

<p>Grupa:</p>
<p id="groupname">brak</p>
<p>Zawodnik:</p>
<p id="contestantno">brak</p>

<table>

	<tr>
		<td>Typ: </td>
		<td>
			<% if (contest.scores == "ten") { %>
				<% if (contest.granulation == "full") { %>
				<input id="typ" type="range" name="points" min="0" max="10">
				<% } else if (contest.granulation == "half") { %>
				<input id="typ" type="range" name="points" min="0.0" max="10.0" step="0.5">
				<% } %>
			<% } else if (contest.scores == "twenty") { %>
				<% if (contest.granulation == "full") { %>
				<input id="typ" type="range" name="points" min="0" max="20">
				<% } else if (contest.granulation == "half") { %>
				<input id="typ" type="range" name="points" min="0.0" max="20.0" step="0.5">
				<% } %>
			<% } %>
		</td>
		<td class="score"><div id="styp"></div></td>
	</tr>

	<tr>
		<td>Głowa i szyja: </td>
		<td>
			<% if (contest.scores == "ten") { %>
				<% if (contest.granulation == "full") { %>
				<input id="glowa" type="range" name="points" min="0" max="10">
				<% } else if (contest.granulation == "half") { %>
				<input id="glowa" type="range" name="points" min="0.0" max="10.0" step="0.5">
				<% } %>
			<% } else if (contest.scores == "twenty") { %>
				<% if (contest.granulation == "full") { %>
				<input id="glowa" type="range" name="points" min="0" max="20">
				<% } else if (contest.granulation == "half") { %>
				<input id="glowa" type="range" name="points" min="0.0" max="20.0" step="0.5">
				<% } %>
			<% } %>
		</td>
		<td class="score"><div id="sglowa"></div></td>
	</tr>

	<tr>
		<td>Kłoda: </td>
		<td>
			<% if (contest.scores == "ten") { %>
				<% if (contest.granulation == "full") { %>
				<input id="kloda" type="range" name="points" min="0" max="10">
				<% } else if (contest.granulation == "half") { %>
				<input id="kloda" type="range" name="points" min="0.0" max="10.0" step="0.5">
				<% } %>
			<% } else if (contest.scores == "twenty") { %>
				<% if (contest.granulation == "full") { %>
				<input id="kloda" type="range" name="points" min="0" max="20">
				<% } else if (contest.granulation == "half") { %>
				<input id="kloda" type="range" name="points" min="0.0" max="20.0" step="0.5">
				<% } %>
			<% } %>
		</td>
		<td class="score"><div id="skloda"></div></td>
	</tr>

	<tr>
		<td>Nogi: </td>
		<td>
			<% if (contest.scores == "ten") { %>
				<% if (contest.granulation == "full") { %>
				<input id="nogi" type="range" name="points" min="0" max="10">
				<% } else if (contest.granulation == "half") { %>
				<input id="nogi" type="range" name="points" min="0.0" max="10.0" step="0.5">
				<% } %>
			<% } else if (contest.scores == "twenty") { %>
				<% if (contest.granulation == "full") { %>
				<input id="nogi" type="range" name="points" min="0" max="20">
				<% } else if (contest.granulation == "half") { %>
				<input id="nogi" type="range" name="points" min="0.0" max="20.0" step="0.5">
				<% } %>
			<% } %>
		</td>
		<td class="score"><div id="snogi"></div></td>
	</tr>

	<tr>
		<td>Ruch: </td>
		<td>
			<% if (contest.scores == "ten") { %>
				<% if (contest.granulation == "full") { %>
				<input id="ruch" type="range" name="points" min="0" max="10">
				<% } else if (contest.granulation == "half") { %>
				<input id="ruch" type="range" name="points" min="0.0" max="10.0" step="0.5">
				<% } %>
			<% } else if (contest.scores == "twenty") { %>
				<% if (contest.granulation == "full") { %>
				<input id="ruch" type="range" name="points" min="0" max="20">
				<% } else if (contest.granulation == "half") { %>
				<input id="ruch" type="range" name="points" min="0.0" max="20.0" step="0.5">
				<% } %>
			<% } %>
		</td>
		<td class="score"><div id="sruch"></div></td>
	</tr>

</table>

<div id="err"></div>
<input type="hidden" id="myname" value="<%= user.username %>" />
<p>Oceniasz jako <%= user.username %></p>
<button id="judge" name="stop" type="button" value="">Oceń</button>
